
// Contains JS shared between nearly all pages
// Includes nav bar, reusable classes, and animations

$(document).ready(function() {

    // Navigation Toggling
    $("a#hamburger-nav").click(function() {
        $("#hamburger-nav-items").toggle();
    })

})
