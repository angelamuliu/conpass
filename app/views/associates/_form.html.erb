<%= simple_form_for(@associate) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :vendor_id %>
    <%= f.input :first_name %>
    <%= f.input :last_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
