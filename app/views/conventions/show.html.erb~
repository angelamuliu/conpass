<div class="main">
    <div class="row con_info">
        <div id = "title" class="col-xs-6">
            <%= @convention.name %>
        </div>
        <div class="col-xs-6 dates">
            <%= @convention.start_date.strftime('%m/%d/%y') %> - <%= @convention.end_date.strftime('%m/%d/%y') %>
	    <%= @convention.start_date.strftime('%l:%M%p') %> - <%= @convention.end_date.strftime('%l:%M%p') %>
        </div>
    </div>

    <div id="sub_header" class="row topbar show_page">
        <div onclick ="maps()" id = "title" class="col-xs-8">
	    <!-- inline styling for javascript, will fix -->
	    <a href="#"id="map_title" style="color:black;">
            Convention Maps
            </a>
        </div>
        <div  onclick ="vendors()" class="title col-xs-4">
            <a href="#"id="vendor_title">
                Vendors
            </a>
        </div>
    </div>

    <ul id="maps" class="maps">
        <% @maps.each do |map| %>
        <li class="row convention">
	    <div class="con_id">
                <%= map.id %>
	    </div>
            <div class="con_image">
                <img style="width:90%;height:70px;" src="http://1.bp.blogspot.com/-Zr0pmj1bLnM/Uhh7kROhGYI/AAAAAAAAGkE/W51xFS75-Ec/s1600/no-thumbnail.png">
            </div>
            <p>
                <h5><%= link_to map.name, map %></h5>
                <%= map.start_date.strftime('%m/%d/%y') %> - <%= map.end_date.strftime('%m/%d/%y') %>
		<%= map.start_date.strftime('%l:%M%p') %> - <%= map.end_date.strftime('%l:%M%p') %>
            </p>
        </li>
        <% end %>
    </ul>

    <ul id="vendors" class="vendors">
        <% @vendors.each do |vendor| %>
        <li class="row convention">
	    <div class="con_id">
                <%= vendor.tags %>
	    </div>
            <div class="col-xs-5 vendor_names">
                <%= link_to vendor.name, map %>
            </div>
            <div class="col-xs-4 vendor_name">
            </div>
            <div class="col-xs-1 vendor_info">
                        <a id="popup" onclick="div_show()" href="#">
            		    <i class="fa fa-map"></i>
            		</a>
	    </div>
            <div class="col-xs-1 vendor_info">
                    <div class="vendor_info vendor_icons">
                        <a id="star<%=vendor.id%>" onclick="star(<%= vendor.id%>)" href="#">
            		    <i class="fa fa-star-o"></i>
            		</a>
	            </div>
	    </div>
        </li>
      <% end %>
    </ul>



<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">

	<div id="openHere">
		<div id="popupSort">
			<div class="popupHeader">Filter</div>
			<form id="form" method="post" name="form">
				<input type="radio" id="r1" name="Sort" value="Current">Current
				<br>
				<input type="radio" id="r2" name="Sort" value="Upcoming">Upcoming
				<div class="row">
					<div class="col-xs-6">
						<div class="submit">
							<a href="javascript:%20sort()" id="submit">Submit</a>
						</div>
					</div>
					<div class="col-xs-6">
						<div class="cancel">
							<a href="#" id="cancel" onclick ="div_hide()">Cancel</a>	
						</div>
					</div>
				</div>
			</form>
		</div>
	<!-- Popup Div Ends Here -->
	</div>
	
	<%= javascript_include_tag "conventions_popup.js" %>
	<div id="sorting_information">
		<div id="current_data">
			<%= @current.to_a %>
		</div>
		<div id="upcoming_data">
			<%= @upcoming.to_s %>
		</div>
	</div>
</div>



